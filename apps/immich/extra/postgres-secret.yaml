# Referenced by both values.yaml (DB_PASSWORD valueFrom) and postgres.yaml
# (as POSTGRES_PASSWORD) - actual secret should be done through GitHub repo secrets
apiVersion: v1
kind: Secret
metadata:
  name: immich-postgres-secret
type: Opaque
stringData:
  password: "CHANGE_ME_STRONG_PASSWORD"   # <-- CHANGE ME
